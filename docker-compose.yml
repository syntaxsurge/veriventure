services:
  veriventure:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "${VERIVENTURE_PORT:-3000}:3000"
    restart: unless-stopped
    environment:
      - NODE_ENV=production
